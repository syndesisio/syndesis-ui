machine:
  node:
    version: 6.9.1
  services:

branches:

dependencies:
  pre:
    - npm install
    - npm update && npm prune && npm run clean
  override:
    - npm run build

test:
  override:
    - rm -Rf coverage
    - npm run test
